public class Lib {

    /**
     * to extract this package as lib, goto setting, project structure,
     * project setting > artifacts new jar and include in project build
     * now with every build in articats folder a jar exists that contains everything
     * even the dependencies!
     */
    public void hi() {
        System.out.println("hi");
    }
}
